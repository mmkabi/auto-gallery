# Build Evidence

## Claim Policy

The precise claim that the complete product was built in under four hours is **not verified**. The approved narrative for this submission is:

> GPT-5.6 and Codex accelerated the transformation from concept to a working end-to-end prototype within a single focused build session.

The 3D asset and portions of the original viewer existed before this competition transformation.

## Verified Evidence

- Competition transformation work occurred in the Codex workspace on 2026-07-20.
- A pre-change backup exists at `backups/competition-preflight-20260720/`.
- Baseline and final build command outputs were captured during the active task.
- The baseline used a raw OBJ/MTL development path and localized product interface.
- New work includes the global Auto Gallery identity, Lyra architecture, strict tool contracts, optimized glTF pipeline, Guided Mode, consent-based lead flow, tests, security controls, static deployment readiness, browser QA, and competition documents.
- The source vehicle asset was pre-existing and was not modeled during this session.

## Missing Evidence

- **TODO(owner):** retrieve the competition event activity record.
- **TODO(owner):** retrieve the Codex task/session identifier from the application history.
- **TODO(owner):** provide the final public repository URL after replacing the placeholder remote if needed.
- **TODO(owner):** identify which inherited viewer changes were completed before the event.

The current workspace is a Git repository on branch `main`; the configured remote is a placeholder and requires owner confirmation before public submission.
